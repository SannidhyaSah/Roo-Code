export const formatResponse = {
    contextTruncationNotice: jest.fn().mockReturnValue("[CONTEXT TRUNCATED NOTICE]"),
    duplicateFileReadNotice: jest.fn().mockReturnValue("[DUPLICATE FILE READ NOTICE]"),
    // Add other methods as needed
};